package org.matrix.bindertest;

/** Entry point named by META-INF/xposed/java_init.list. Never loaded: this module has no scope. */
public class Entry {
    public Entry(Object base, Object param) {}
}
